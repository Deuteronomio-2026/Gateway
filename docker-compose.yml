services:
  gateway:
    build: .
    ports:
      - "8080:8080"
    environment:
      - EUREKA_CLIENT_ENABLED=false
    depends_on:
      - eureka

  eureka:
    image: steeltoeoss/eureka-server
    ports:
      - "8761:8761"