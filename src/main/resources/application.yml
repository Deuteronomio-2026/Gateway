spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes: []  # las rutas las definimos después

server:
  port: 8080

eureka:
  client:
    enabled: false  # desactivar hasta tener Eureka corriendo

management:
  endpoints:
    web:
      exposure:
        include: health, info